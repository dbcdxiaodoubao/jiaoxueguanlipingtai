<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.mapper.SubjectsMapper">

    <select id="list" resultType="subjectsListVo">
        SELECT
            t1.subject_id,
            t1.subject_name,
            t1.grade
        FROM
            ms_subjects AS t1
        WHERE t1.del_flag = 0
        <if test="grade != null and grade != 0 ">
           AND t1.grade = #{grade}
        </if>
    </select>

    <select id="selectBySubjectNameGrade" resultType="Integer">
        SELECT
            COUNT(*)
        FROM
            ms_subjects AS t1
        where t1.subject_name = #{subjectName} And t1.grade = #{grade} And t1.del_flag = '0'
    </select>
</mapper>
