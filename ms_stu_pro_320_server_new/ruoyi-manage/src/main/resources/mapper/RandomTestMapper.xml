<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.mapper.RandomTestMapper">
  <select id="listRandomTests" resultType="randomTestVo">
    SELECT
      t1.random_test_id,
      t1.test_name,
      t1.create_time,
      t2.subject_name
    FROM
      ms_random_test AS t1
        JOIN ms_subjects AS t2 ON t1.subject_id = t2.subject_id
    WHERE
      t1.del_flag = 0
      AND t1.user_id=#{userId}
  </select>
</mapper>
